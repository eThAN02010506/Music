# Leave empty to use the same-origin /api proxy configured by Vite.
# Set an absolute URL only when the frontend and API are deployed separately.
VITE_API_BASE_URL=

# Optional development proxy target when port 8000 is occupied locally.
MUSIC_INSIGHT_API_PROXY_TARGET=http://127.0.0.1:8000
